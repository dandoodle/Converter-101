ifelse (Choice==1){ //When the user puts in 1 it comes up with the m to feet converter
        System.out.print("Enter Value: ");
        double m = user_input.nextDouble();
        double answer = m * convert[1];
        System.out.println(" "+val);
}
