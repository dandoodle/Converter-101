package converterassessment;

import java.util.Scanner;
public class ConverterAssessment {

    public static void main(String[] args) {
    //declaring of variables ,,,
       int choice=-1;               //menu selector
       double x;                    //ctof userInput value
       double userInputValue;       
       double value;                //output value
       double [] convert=new double[5]; //array
       
       //start of methods..
       
       while (choice==-1){    //this will run the MENU method       
         choice = menu();  //the whole point of the menu it to get the users input and re direct them to the appropriate place
        //this is saying that the value returned from MENU==choice (it will redirect to users choice)              
       } 
       /**
       * your method thingy here
       **/
       
       
       if(choice==2){   //this will run the celcius to fahrenheit converter
       x = ctof();      //ctof is the name of my method. i am only wanting to return 'x' (users input) from the method
       
       convert[2]=x*9/5+32; //this declares value[2] from my array, it uses 'x' in the equation
        System.out.println(x+" C = "+convert[2]+" F"); //prints x's original value then what the value is equal to in fahrenheit
       }
